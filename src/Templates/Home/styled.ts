import styled from "styled-components";

export const ContainerMain = styled.div`

  >div {
    display: flex;
    gap: 20px;

    >button {
      padding: 20px;
      border: none;
      background-color: black;
      color: #fff;
      cursor: pointer;
    }
  }
`